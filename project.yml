name: WhatsAppHD
options:
  bundleIdPrefix: com.studio
  deploymentTarget:
    iOS: "16.0"
targets:
  WhatsAppHD:
    type: application
    platform: iOS
    sources: [Sources]
    info:
      path: Sources/Info.plist
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.studio.whatsapphd
      TARGETED_DEVICE_FAMILY: "1"
      GENERATE_INFOPLIST_KEY_UILaunchScreen_Generation: "YES"
      ASSETCATALOG_COMPILER_APPICON_NAME: ""
